export enum ProjectStatus {
  active = "active",
  archived = "archived",
}

export enum TaskStatus {
  created = "just created",
  inProgress = "in progress",
  finished = "finished",
  cancelled = "cancelled",
}
