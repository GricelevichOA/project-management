export enum ProjectStatus {
  created = "created",
  inProgress = "inProgress",
  finished = "finished",
  paused = "paused",
}

export enum TaskStatus {
  created = "created",
  inProgress = "inProgress",
  finished = "finished",
  paused = "paused",
  overdue = "overdue",
}
